# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: df49b3f2-a978-4a93-a935-6e73a7bd9e39

include:
  - resources/*.yml
  - resources/*/*.yml



# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405614548589257.17.azuredatabricks.net/
    variables:
      catalog: databricks_cata
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-7405614548589257.17.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/manasmandal14@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/manasmandal14@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricks_cata
      schema: prod
    permissions:
      - user_name: manasmandal14@gmail.com
        level: CAN_MANAGE
